name: az

on:
  push:
    branches:
      - main
    tags:
      - az:*
    paths:
      - az/**
  pull_request:
    paths:
      - .github/workflows/_build.yaml
      - .github/workflows/az.yaml
      - az/**

jobs:
  build-docker-image:
    uses: ./.github/workflows/_build.yaml
    with:
      path: az
    secrets: inherit
