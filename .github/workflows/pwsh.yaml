name: pwsh

on:
  push:
    branches:
      - main
    tags:
      - pwsh:*
    paths:
      - pwsh/**
  pull_request:
    paths:
      - .github/workflows/_build.yaml
      - .github/workflows/pwsh.yaml
      - pwsh/**

jobs:
  build-docker-image:
    uses: ./.github/workflows/_build.yaml
    with:
      path: pwsh
    secrets: inherit
