name: powershell-sdk

on:
  push:
    branches:
      - main
    tags:
      - powershell-sdk:*
    paths:
      - powershell-sdk/**
  pull_request:
    paths:
      - .github/workflows/_build.yaml
      - .github/workflows/powershell-sdk.yaml
      - powershell-sdk/**

jobs:
  build-docker-image:
    uses: ./.github/workflows/_build.yaml
    with:
      path: powershell-sdk
    secrets: inherit
