@{
    Labels = @{
        "org.opencontainers.image.title" = "AWS"
        "org.opencontainers.image.description" = "AWS Tools for PowerShell and Git on Alpine Linux, for provisioning AWS resources and deploying applications to AWS"
    }
    Platforms = @("linux/amd64")
}